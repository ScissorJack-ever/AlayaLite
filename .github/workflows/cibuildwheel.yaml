---
name: Build Wheels

on  :
  workflow_dispatch:
  push:
    branches: [test/ci]
  #   tags:
  #     - 'v*'

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }} (${{ matrix.build_id }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
        - os: ubuntu-latest
          arch: x86_64
          build: cp3{8,9,10,11,12,13}-*
          build_id: all
        - os: ubuntu-24.04-arm
          arch: aarch64
          build: cp3{8,9,10,11,12,13}-*
          build_id: all
        - os: macos-15-intel
          arch: x86_64
          build: cp3{8,9,10,11,12,13}-*
          build_id: all
        - os: macos-14
          arch: arm64
          build: cp3{8,9,10,11,12,13}-*
          build_id: all
        - os: windows-2022
          arch: AMD64
          build: cp3{8,9,10,11,12,13}-*
          build_id: all

    env:
      CONAN_NON_INTERACTIVE: 1
      # CI specific config: mount Conan, uv, and pip cache in Linux Docker
      CIBW_CONTAINER_ENGINE: 'docker; create_args: -v /home/runner/.conan2:/root/.conan2 -v /home/runner/.cache/uv:/root/.cache/uv
        -v /home/runner/.cache/pip:/root/.cache/pip'
      CIBW_BUILD: ${{ matrix.build }}
      CIBW_ARCHS_LINUX: ${{ matrix.arch }}
      CIBW_ARCHS_MACOS: ${{ matrix.arch }}
      CIBW_ARCHS_WINDOWS: ${{ matrix.arch }}

    steps:
    - uses: actions/checkout@v4

    - name: Set up uv
      uses: astral-sh/setup-uv@v5
      with:
        cache-local-path: ${{ runner.os == 'Linux' && '/home/runner/.cache/uv' || '' }}

    - name: Restore Conan cache
      uses: ./.github/actions/cache-restore

    - name: Ensure pip cache directory exists
      if: runner.os == 'Linux'
      run: mkdir -p /home/runner/.cache/pip

    - name: Restore pip cache
      if: runner.os == 'Linux'
      uses: actions/cache@v4
      with:
        path: /home/runner/.cache/pip
        key: pip-${{ runner.os }}-${{ matrix.arch }}-${{ hashFiles('pyproject.toml', 'uv.lock') }}
        restore-keys: |
          pip-${{ runner.os }}-${{ matrix.arch }}-
          pip-${{ runner.os }}-

    - name: Build wheels
      uses: pypa/cibuildwheel@v3.3

    - name: Upload wheels artifact (attempt 1)
      id: upload_wheels_1
      uses: actions/upload-artifact@v4
      continue-on-error: true
      with:
        name: cibw-wheels-${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.build_id }}
        path: ./wheelhouse/*.whl
        compression-level: 0
        overwrite: true

    - name: Wait before artifact upload retry
      if: steps.upload_wheels_1.outcome == 'failure'
      run: sleep 15

    - name: Upload wheels artifact (attempt 2)
      id: upload_wheels_2
      if: steps.upload_wheels_1.outcome == 'failure'
      uses: actions/upload-artifact@v4
      continue-on-error: true
      with:
        name: cibw-wheels-${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.build_id }}
        path: ./wheelhouse/*.whl
        compression-level: 0
        overwrite: true

    - name: Wait before final artifact upload retry
      if: steps.upload_wheels_1.outcome == 'failure' && steps.upload_wheels_2.outcome == 'failure'
      run: sleep 30

    - name: Upload wheels artifact (attempt 3)
      id: upload_wheels_3
      if: steps.upload_wheels_1.outcome == 'failure' && steps.upload_wheels_2.outcome == 'failure'
      uses: actions/upload-artifact@v4
      continue-on-error: true
      with:
        name: cibw-wheels-${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.build_id }}
        path: ./wheelhouse/*.whl
        compression-level: 0
        overwrite: true

    - name: Fail if artifact upload never succeeded
      if: always() && steps.upload_wheels_1.outcome == 'failure' && steps.upload_wheels_2.outcome == 'failure' && 
        steps.upload_wheels_3.outcome == 'failure'
      run: |
        echo "::error::Artifact upload failed after 3 attempts."
        exit 1

  # upload_pypi:
  #   needs: [build_wheels]
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
  #   steps:
  #     - uses: actions/download-artifact@v4
  #       with:
  #         pattern: cibw-wheels-*
  #         path: dist
  #         merge-multiple: true

  #     - uses: pypa/gh-action-pypi-publish@release/v1
  #       with:
  #         password: ${{ secrets.PYPI_API_TOKEN }}
